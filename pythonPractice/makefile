# Makefile for Python project

PYTHON = python3
SCRIPT = main.py

# Run the Python script
run:
	$(PYTHON) $(SCRIPT)

# Clean doesn't apply to Python, but you can use it for other cleanup tasks
clean:
	@echo "No clean-up necessary for Python"

# Test the Python script (if you have any test cases)
test:
	$(PYTHON) -m unittest discover